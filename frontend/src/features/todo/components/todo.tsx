import { Table } from "@/components";

export function Todo() {

    return (
        <div
            className="w-full min-h-full"
        >
            <Table
                data={[]}
                columns={[]}
            />
        </div>
    );
}