---
name: commit-message
description: |
  コード実装・ファイル修正を伴う作業が完了した直後に、過去のコミット履歴のスタイルに沿ったコミットメッセージを提案する。

  以下のような場合に使用する：
  - コードの新規作成・修正が完了したとき
  - 「コミットメッセージを考えて」「コミットメッセージを提案して」「コミット文を作って」など、コミットメッセージの作成を求められたとき

  以下の場合は使用しない：
  - 調査・説明・設計相談のみの場合
  - コミット操作そのものを求められた場合（コミットは必ずユーザーが行う）
version: 1.0.0
---

# Commit Message Skill

## Overview

実装完了後に、過去のコミット履歴のスタイルを参照して一貫性のあるコミットメッセージを提案する。

**重要：このスキルはメッセージを提案するだけであり、`git commit` / `git add` などのコミット操作は絶対に行わない。コミットはユーザーが自分で実施する。**

---

## Procedure

1. `git log --oneline` で全コミットメッセージを確認し、スタイル・語調・粒度を把握する
2. `git diff HEAD` で変更内容を確認する
3. 過去のスタイルに沿ったコミットメッセージを1〜3件候補として提案する
4. コミットメッセージが英語の場合は日本語訳も併記する
5. コミット操作は一切行わない

---

## Output Format

英語の場合：

```
## コミットメッセージ候補

1. [英語メッセージ]（[日本語訳]）
2. [英語メッセージ]（[日本語訳]）（任意）
3. [英語メッセージ]（[日本語訳]）（任意）
```

日本語の場合：

```
## コミットメッセージ候補

1. [候補1]
2. [候補2]（任意）
3. [候補3]（任意）
```
