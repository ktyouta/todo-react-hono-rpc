# Expected Output — Bug Fix

---

## ケース1: バックエンドのバグ修正（全ステップ）

### 状況
- タスク削除 API で「サブタスクが残っているタスクでも削除できてしまう」バグ

### Step 1 出力（原因調査）

```
## 原因調査結果

### 再現条件
- サブタスクが1件以上あるタスクを削除しようとすると、サブタスクだけが孤立レコードとして残る

### 原因箇所
- ファイル: backend/src/api/todo/service/delete-todo.service.ts:18
- 原因: `db.batch()` にサブタスク削除のクエリが含まれておらず、タスク本体のみが削除されている

### 根本原因
- サブタスク削除処理が実装当初から漏れており、タスク削除時にカスケード削除される前提のテストも存在しなかった
```

### Step 3〜5 中間確認

```
テスト作成完了。`npm run test` でテスト失敗を確認しました（期待通り）。
修正を適用し、テストが通過することを確認しました。
```

### Step 7 出力（レビュー）

```
## Backend Review 結果

チェック完了。問題なし。

## Architecture Review 結果

チェック完了。問題なし。

## Comments Review 結果

チェック完了。問題なし。
```

---

## ケース2: フロントエンドのバグ修正（横展開あり）

### 状況
- タスク一覧画面のアクションバーで `border-orange-200` が消えているバグ

### Step 1 出力

```
## 原因調査結果

### 再現条件
- タスク一覧画面でアクションバーを表示するとボーダーが見えない

### 原因箇所
- ファイル: frontend/src/features/todo/components/todo-action-bar.tsx:12
- 原因: 前回のスタイル修正で `border-orange-200` がベースクラスから削除されている

### 根本原因
- クラス統合時に意図せず `border-orange-200` を削除した
```

### Step 6 出力（横展開）

```
## 水平展開漏れチェック結果

漏れなし。水平展開は完了しています。
```
