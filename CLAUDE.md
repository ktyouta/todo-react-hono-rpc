# プロジェクト概要

React + Hono + RPC を用いたフロントエンド・バックエンド統合Todoアプリ

* フロントエンド：React（Vite）
* バックエンド：Hono（Cloudflare Workers 前提）
* 通信方式：Hono RPC（型安全通信）
* データベース：Cloudflare D1（SQLite）+ Drizzle ORM

設計の分かりやすさ・拡張のしやすさを最優先とする。

---

# 主要コマンド

## フロントエンド (`frontend/`)

* `npm run dev`: 開発サーバー起動
* `npm run test`: テスト実行
* `npm run storybook`: Storybook 起動

## バックエンド (`backend/`)

* `npm run dev`: 開発サーバー起動
* `npm run test`: テスト実行
* `npm run db:generate`: マイグレーション SQL 生成
* `npm run db:migrate:local`: ローカル D1 にマイグレーション適用
* `npm run db:migrate:prod`: 本番 D1 にマイグレーション適用
* `npm run db:seed:local`: ローカル D1 に Seed データ投入
* `npm run deploy:prod`: 本番環境にデプロイ

---

# 新機能追加時の進め方

1. 調査
2. 計画
3. 実装
4. テスト

---

# バグ修正時の進め方

1. 原因調査
2. テスト作成（TDD）
3. 修正実装
4. 動作確認

---

# コーディング規約（共通）

* TypeScript の厳密モードを使用する
* 関数・コンポーネントは function 宣言で定義する
* スタイルは Tailwind CSS のみを使用する
* テストは Vitest で記述する
* state・メソッドには必ずコメントを付ける（state は `//`、メソッドは `/** */` JSDoc）
  * コメントには「何をするか」のみを書く。「どこから取得するか・どのように実装するか」という実装詳細は書かない
    * 良い例: `// タスク一覧を取得`
    * 悪い例: `// タスク一覧を取得: repository経由でDBから取得`（`: repository経由でDBから取得` が実装詳細）

---

## 指示・回答について

* すべての応答を生成する前に、`.claude/skills/*/SKILL.md` を Glob してプロジェクト内の利用可能なスキル一覧を取得し、ユーザーの発話・状況と照合すること。合致するスキルがある場合、Skill ツールの呼び出しを最初のアクションとする。この照合を省略してはならない。system-reminder のスキル一覧だけを参照するのは不十分（初回呼び出しのスキルは system-reminder に載らないため）
  * Glob を実行する際は **必ず `path` パラメータにリポジトリルートを指定すること**。作業ディレクトリがサブフォルダの場合、path 未指定では `.claude/` が見つからない。リポジトリルートは `git rev-parse --show-toplevel` で取得する
  * Glob の結果が空でも「存在しない」と即断しないこと。ファイルパスが既知の場合は Read で存在を二重確認すること（Glob はパス区切り・大文字小文字の違いで検出漏れが発生する）
* セッション中に `.claude/skills/` 内のファイルが追加・修正された場合は、必ず該当ファイルを Read で読み込み、内容を把握してから次の行動に移ること。この読み込みを省略してはならない
* スキルを実行する前に `.claude/incidents.md` を Read し、実行するスキル名タグ（例: `#feature-impl`）のインシデントを確認すること。ファイルが存在しない場合は `.claude/incidents-template.md` を元に作成してから続行すること
* ユーザーからの修正・指摘発話（typo・細かい文言調整を除く）を検知したとき、次のように確認すること：「今回の指摘を #[スキル名] のインシデントとして記録しますか？（記録内容: [指摘の要約を1行で]）」
* 回答は日本語で行うこと
* 指示をする側も人間であり、誤りが含まれる可能性がある
  * 指示内容は批判的に検討し、誤りがあれば指摘すること
* 指示内容を鵜呑みにせず、背景や説明不足を感じた場合は確認を取ること
* 指示された案以外にも選択肢がないか検討し、より良い案があれば提案すること
* 「ファイルの修正不要」「修正なし」など、ファイルの修正が不要と示唆する指示があった場合は、ファイル操作（作成・変更）を行わず、会話のみで回答すること（読み込みは許可）
* 「ファイル操作不要」など、ファイル操作全般を行わないよう示唆する指示があった場合は、一切のファイル操作（読み込み・作成・変更）を行わず、会話のみで回答すること
  * 依頼内容がファイル操作を伴う場合（例：「Skill に保存して ※ファイルの修正不要」）は、矛盾を指摘して確認を取ること
* 既存の skill / agent を修正・流用する提案をする場合は、必ず事前にその内容を Read で確認してから判断すること
* 設計・実装の方針を提案する前に、プロジェクト内の類似パターンや既存の実装を確認してから判断すること
  * 例：自動トリガーの実装方法を提案するなら、既存の自動トリガースキルのパターンを先に確認する

---

## 設計指針

* いきなりコードを書き始めず、まず設計案を提示すること
* 設計検討時は以下を順守する
  * 複数案を提示し、それぞれのメリット・デメリットを併記する
  * 現在の目的に対して最もシンプルかつメンテナンス性の高い案を1つ選定する
  * 将来どのような変更が入るとその案が破綻するかも併せて説明する
  * 症状への対処ではなく根本原因を解消する設計を選ぶこと

---

## RPC 設計指針（重要）

* バックエンドは REST API の URL 設計を前提とする
* フロントエンドは URL や HTTP メソッドを意識しない（RPC クライアント経由で呼び出す）
* RPC の型定義は **バックエンドを単一の source of truth** とする
* フロントエンドで API 用の型を新規定義しない
* Hono の app / router 定義から RPC クライアントを生成する
* fetch / axios を直接使用しない（ファイルアップロード等の例外を除く）
* RPC レスポンスに対して `as` による型アサーションを使用しない
  * `InferResponseType` / `InferRequestType` 等で型推論し、RPC の型安全性を維持する
  * アサーションが必要になる場合はバックエンドのレスポンス型定義に問題がある可能性を疑うこと
  * RPC以外の箇所についても型アサーションが必要になる場合は修正前に相談すること
* **バックエンドに `@/` パスエイリアスを追加しないこと（相対パスを使う）**
  * フロントエンドの tsconfig が `@/*` → `frontend/src/*` にマッピングしている
  * バックエンドに同様のエイリアスを追加すると、RPC 型チェーンでフロントエンド tsc がバックエンドファイルを処理する際に誤解決される
  * バックエンドの import は必ず `../../../domain` のような相対パスで記述する

---

## フロントエンド固有の設計指針

### API クライアントの使い分け

* 通常の API 呼び出しは `lib/rpc-client.ts` の `rpc` を使用する
* `lib/api-client.ts`（Axios）はリフレッシュトークンのエンドポイント専用
  * `rpc-client.ts` → `refresh-handler.ts` → `rpc-client.ts` の循環参照を避けるため
* 新しい API エンドポイントを追加する際は `rpc` を使うこと

### features フォルダ内部構造ルール

* `features/[機能名]/` 単位でフォルダを作る（kebab-case）
  * 操作の性質が異なるものは別フォルダとして独立させる
* 複数 feature で共有する API（マスターデータ等）は `features/api/` に置く

#### api/ フォルダ
* API 呼び出しは **1操作1ファイル** で分割する（例: `get-todo.ts`, `create-todo.ts`）
* React Query のクエリキーは **`query-key.ts`** に集約する

#### components/ フォルダ — Container/Presentational パターン
* `[機能名]-page.tsx` — ルーティングのエントリポイント
* `[機能名]-[部位]-container.tsx` — データ取得・状態管理を担う（`-container` サフィックス必須）
* `[機能名]-[部位].tsx` — 表示のみ（state・副作用・データフェッチを持たない）

#### hooks/ フォルダ
* **1責務1ファイル** で分割する
* フォーム専用 hook は `use-[機能名].form.ts` とする

#### 省略可能なフォルダ
| フォルダ | 追加する条件 |
|---|---|
| `types/` | feature 固有の型定義があるとき |
| `constants/` | feature 固有の定数があるとき |
| `utils/` | feature 固有のユーティリティがあるとき |

### コメント規約（フロントエンド）

* `useState` / `useRef` / `useMemo` / カスタム hooks の返り値などの state 系変数は全件コメントを書く
  * コメントには変数が「何を表すか」を書く。実装詳細（どこから取得するか等）は書かない
* props 型は `type PropsType = Pick<XxxType, '...'>` 形式で定義する
* JSX 内のセクション区切りは `{/* セクション名 */}` で行う

### アイコン

* アイコンは `react-icons` を使用する（インライン SVG を直接記述しない）

### sample 機能

* `features/sample/` はリファレンス実装（Container / Presentational パターンの参考）
* 実際のプロジェクトでは削除または置き換える想定

---

## バックエンド固有の設計指針

### 環境変数の扱い（EnvConfig）

* 環境変数は `createEnvConfig(c.env)` ファクトリ関数で readonly オブジェクトに変換する
* ミドルウェア（`envInitMiddleware`）でリクエストごとに `c.set('envConfig', ...)` に格納する
* コントローラーは `c.get('envConfig')` で取得し、ドメインオブジェクトに引数として渡す
* グローバルシングルトンを使わない（Workers はリクエスト間で状態を共有しないため）

### 環境変数ファイルの使い分け

* `wrangler.jsonc` の `vars`: 非機密の設定値（トークン有効期限、CORS オリジン等）
* `backend/.dev.vars`: 機密情報（JWT 秘密鍵、PEPPER 等）— Git 管理しない
* `.dev.vars` は dotenv 形式のため、すべての値が文字列になる
* `.dev.vars` と `wrangler.jsonc` に同名の変数がある場合、`.dev.vars` が優先される

### api フォルダ構成ルール

* `src/api/[機能グループ名]/` 単位でフォルダを作る
  * 操作の性質が異なるものは別フォルダとして独立させる
* 各レイヤーのファイルは **`[操作名].[レイヤー].ts`** で命名し、エンドポイント（操作）単位で分割する
  * 例: `get-todo.controller.ts` / `create-todo.repository.ts`
* ルーター集約ファイルのみ **`[機能グループ名].controller.ts`** とする（例外）
  * 各エンドポイントのコントローラーを集約して Hono ルーターに登録する役割
* repository には必ず対応する **`.repository.interface.ts`** をセットで作成する
  * Repository ファイルはエンドポイント単位で作成し、そのエンドポイントで使う全DB操作（SELECT / INSERT / UPDATE 等）を1ファイルに集約する
  * 操作の種別でファイルを分けない
  * 異なるエンドポイント間で同じDB操作が重複しても共通化しない
* Cloudflare D1 は `db.transaction()` をサポートしない。複数操作をアトミックに実行する唯一の手段は `db.batch([])` である
  * 同一 Repository 内の複数操作: Repository メソッド内で `db.batch([])` を使う
  * 複数 Repository をまたぐ場合: Controller で `db.batch([])` を使う（Service に持たせる必然性がないため）
* Service メソッドとして切り出す判断基準：ロジック（条件分岐・ループ・計算・DB アクセス等、例示であって網羅ではない）を含む処理は Service に切り出す。ロジックを含まない単純な構築・変換（例: `new Entity(...)`, `TaskId.of(...)` 等）は Controller で直接行う
* 各レイヤーフォルダに `index.ts` バレルエクスポートを置く
* 以下のレイヤーは必要な場合のみ追加する（不要なら作らない）

| レイヤー | 追加する条件 |
|---|---|
| `schema/` | リクエストのバリデーションがあるとき |
| `dto/` | レスポンスの整形が必要なとき |
| `service/` | ロジック（条件分岐・ループ・計算・DB アクセス等、例示であって網羅ではない）を含む処理があるとき |
| `entity/` | DB 結果をドメイン型に変換するとき |

### コメント規約（バックエンド）

* クラス・メソッドには JSDoc 形式でコメントを書く（`@param` / `@returns` を含む複数行形式）
  ```ts
  /**
   * 説明
   * @param userId
   * @returns
   */
  ```
* コントローラーで処理ブロックが長くなる場合、`// セクション名` でブロックを区切る（実装詳細は含めない）

### DB マイグレーション規約

* `schema.ts` を変更したら必ず `npm run db:generate` を実行し、生成された SQL ファイルとスナップショットをセットで扱う
* `drizzle/*.sql` を手動で新規作成・編集してはならない
* `db:generate` が対応できない変更（SQLite の制約上 DROP COLUMN 等）は `db:generate --custom` で空の SQL ファイルを生成してから SQL を記述する（スナップショットを進める義務がある）

### Zod バージョン

* フロントエンド: Zod v4（`@hookform/resolvers@5.x` が Zod v4 のみ対応）
* バックエンド: Zod v3（`@hono/zod-validator@0.4.x` が Zod v3 のみ対応）
* 両者のバリデーションスキーマは RPC を通じて直接共有しないため、バージョンの違いは問題にならない
* 依存パッケージ更新時は各バリデータライブラリの Zod 対応バージョンを必ず確認すること

---

## コードレビュー指針

* コードレビュー時は、結論だけでなく調査プロセスも共有すること
* 結論には、それを裏付ける重要な根拠を併せて提示すること
* 常に **最新のファイル内容** を前提にレビューを行うこと
* レビュアーと実装者が一緒に妥当性を確認する姿勢を取ること

---

## 実装指針

* 段階的に進める（小さな変更を積み重ねる）
* 複数タスクを同時並行で進めない
* エラーは必ず解消してから次へ進む
* エラーを無視して進めない
* 指示にない機能を勝手に追加しない
* Presentational コンポーネント実装後は、state・イベントハンドラが含まれていないか確認すること

---

## エラー対応指針

* 「なぜ期待と異なる挙動が起きたのか」を必ず深掘りしてから次の対応を検討する

---

# 注意事項（共通）

* 環境変数は .env / .dev.vars で管理する（コミット禁止）
* 以下のファイルの参照・出力・実行を禁止する
  * .env
  * *.env
  * .env.*
  * .dev.vars
* 秘密情報（APIキー、トークン、認証情報）を推測しない
* 明示的な指示がない限り、ファイルの作成・修正・削除を行わない
* GitHub に含めるべきでないファイルを作成する場合は、必ず .gitignore に追加する

## 修正作業時のルール

* 修正による影響範囲を慎重に確認すること
* 他の挙動にも修正が必要な場合は、既存の期待動作が保たれるよう対応すること
* 修正内容について **コミットは実施しない**

## その他

* 指示なくビルドを実行しないこと
* 指示なく dev サーバーの起動・ブラウザでの動作確認を行わないこと（UI確認が必要な場合も、実行前に必ずユーザーの許可を得ること）
